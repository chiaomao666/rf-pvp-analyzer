# 匿名瀏覽器驗證紀錄

- 開發版 `/import` 在未登入狀態直接顯示匯入表單，未導向 Manus 登入。
- 頁首顯示「本機私有資料 / THIS BROWSER ONLY」。
- 已填入一筆匿名匯入驗證資料，下一步將提交後確認建立、列表與刪除流程。
- 匯入成功：顯示新建 1 筆、回填 0 筆、未建立 0 筆，未出現登入導向。
- `/matches` 顯示匿名匯入的 1v1 待確認戰績，並出現直接刪除按鈕。
- 初版原生瀏覽器確認曾使自動化工作階段逾時並回到空白頁；該限制已以頁內可存取確認對話取代。
- 在使用者確認後，使用同一未登入瀏覽器的匿名裝置識別刪除測試戰績 `id: 90001`；伺服器回傳 HTTP 200 與 `{ success: true }`。
- 重新開啟 `/matches` 後仍未登入，列表顯示找不到戰績，確認匿名刪除完成且沒有登入導向。
- 後續建立的匿名測試紀錄可在 `/matches` 列表看見垃圾桶所在的獨立操作欄；原生確認對話的自動化限制促成改為頁內對話。
- 匿名 owner 範圍、刪除結果與列表重載亦由路由回歸及同源匿名刪除驗證覆蓋。
- 2026-08-24 21:37（本機顯示）在未登入瀏覽器以 `id: 120001` 完成真實 UI 驗證：點擊垃圾桶後網址維持 `/matches`，頁內顯示「刪除這筆戰績？」對話及「取消／確認刪除」按鈕；先按取消後紀錄仍存在。
- 再次開啟對話並按「確認刪除」後，按鈕顯示「刪除中…」，成功後對話關閉、網址仍為 `/matches`，列表立即顯示「找不到符合條件的戰績」，且全程未導向 Manus 登入。
