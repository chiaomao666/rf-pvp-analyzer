# 純前端 IndexedDB 驗證紀錄

## 使用者實際備份還原

- 驗證日期：2026-08-24（使用者本機備份）
- 輸入格式：`rf-pvp-analyzer/local-backup-v1`
- 宣告／實際戰績筆數：11／11
- 本機 IndexedDB 還原結果：11 筆已還原、0 筆略過
- 還原後介面總覽：累計 11 場、可判定勝率 57.1%、勝／敗 4／3、目前排名 #668
- 匯入批次：0 筆；這與使用者備份內容相符。

此次驗證只保存筆數與統計摘要；使用者原始隊伍、對手與封包資料未寫入專案。

## 回歸驗證

- `pnpm test`：16 項測試通過（包含 IndexedDB 完整備份還原／自動編號案例）。
- `pnpm check`：通過。
- `pnpm build`：已在靜態化後通過；更動還原鍵值修正後將於交付前重新執行。

## 本機工作流程回歸

- 手動建立：以明確標示的暫存紀錄驗證，新增後可導向詳情，首頁累計由 11 增至 12；取消刪除後紀錄仍存在，確認刪除後列表與首頁統計回復為 11。
- 一般 JSON 匯入：以不含真實資料的一筆守衛格式 JSON 驗證，匯入摘要成功新增 1 筆，資料可跨頁在列表讀取；確認刪除該測試紀錄後，首頁回復為 11 筆。
- 最終 localhost 總覽：累計 11 場、可判定勝率 57.1%、勝／敗 4／3、目前排名 #668，與使用者實際備份還原摘要一致。
- 正式靜態產物：使用 `pnpm preview` 載入 Hash 路由，完成暫存手動建立、取消／確認刪除，以及小型可攜備份的可見還原與清理；驗證不依賴 Express、tRPC、OAuth 或資料庫。

## 正式靜態預覽最終可見驗證

- 驗證位置：`http://localhost:4173`（`pnpm preview`）；此 origin 的 IndexedDB 與開發站 `http://localhost:5173` 分離，未載入使用者實際備份。
- 起始與清理後總覽：累計 **0** 場、可判定勝率 **—**、目前排名 **—**、勝／敗 **0／0**；戰績列表顯示無符合條件的戰績。
- 一般 JSON 匯入：貼上一筆不含真實資料的守衛格式 `1v1` 勝利紀錄，畫面回報「辨識 1 筆資料：新建 1 筆、更新 0 筆，另有 0 筆未建立」。
- 列表與詳情：列表顯示該筆 `Preview Pilot` 對 `Preview Rival`、排名 `#1000 → #999`；直接載入 `#/matches/4` 可讀取隊伍、排名與保留的原始 JSON，證實 Hash 詳情路由與 IndexedDB 單筆讀取正常。
- 刪除防呆與清理：列表刪除對話框按「取消」後紀錄仍存在；詳情頁按「確認刪除」後回到空列表，總覽重回 **0** 筆。預覽測試紀錄均已清除。
- 開發站隔離核對：清理正式預覽後重新開啟 `http://localhost:5173/#/`，仍可見使用者已驗證的 **11** 筆、**57.1%**、**4／3**、**#668**，未受 preview 測試影響。

## 備份入口文案核對

- `#/import` 可見「守衛 JSON 一般匯入」標題、PVP 守衛 JSON 檔案載入提示，以及「完整本機備份請用上方『還原本機備份』」的 textarea 提示。
- 同頁「完整備份」說明明確標示：`local-backup-v1` 必須透過「還原本機備份」入口處理，才能連同匯入歷程一併還原；一般 JSON 匯入只建立或更新戰績。

## GitHub Pages 子路徑預覽備註

- 在 `GITHUB_ACTIONS=true` 產出的靜態檔案後，原先啟動的 4173 預覽程序於 `/rf-pvp-analyzer/` 顯示空白頁；因此最終核對改以重新啟動、明確對應子路徑的靜態伺服器進行，避免把舊預覽程序或錯誤根路徑的結果誤判為部署輸出。
- 建置檔改以 `/rf-pvp-analyzer/` 掛載後，HTML 與入口 JavaScript 均回應 `200`；靜態站 `http://localhost:4174/rf-pvp-analyzer/#/import` 已可載入完整 UI。完整備份與一般檔案載入器均為檔案欄位，驗證時需依 DOM 順序選用前者，不能把一般 JSON 載入器的文字填入視為完整還原成功。
- 在上述 Pages 結構以不含真實資料的 `local-backup-v1` 測試檔進行實際還原後，畫面顯示 1 筆隨附匯入歷程（已辨識 1、未建立 0）。總覽顯示累計 1、可判定勝率 100%、勝／敗 1／0、目前排名 #699；`#/matches/1` 詳情可讀出雙方隊伍、#700 → #699 與測試備註。測試資料仍將在本次最終驗證結束前清除。
- 詳情頁「刪除紀錄」會顯示頁內確認對話框；按「取消」後紀錄仍留在 `#/matches/1`，並可再次讀到相同的隊伍、排名與備註。
- 再次開啟對話框並按「確認刪除」後，頁面導回 `#/matches`，顯示「已刪除這筆本機戰績」與空列表；GitHub Pages 結構下的測試戰績已清除。
- 刪除後 `#/` 顯示累計場次 0、可判定勝率「—」、目前排名「—」、勝／敗 0／0，證實該靜態預覽的測試資料已完全清理；此 origin 的資料與開發版 5173 的使用者 11 筆資料相互隔離。
- 在同一靜態輸出以手動表單新增一筆無敏感測試戰績（1v1 勝利、#600 → #598）後，首頁實際更新為累計 1、可判定勝率 100%、目前排名 #598、勝／敗 1／0；近期戰績連結為 `#/matches/2`，確認手動建立與統計更新均由此 origin 的 IndexedDB 完成。
- 接著在「守衛 JSON 一般匯入」貼入一筆無敏感資料的 PVP 格式 JSON；頁面顯示「辨識 1 筆資料：新建 1 筆、更新 0 筆，另有 0 筆未建立」，最近匯入批次也出現「PVP 守衛 JSON 匯出／1 已辨識／0 未建立」。此驗證採一般匯入入口，並非完整備份還原。
- 匯入後首頁為累計 2、可判定勝率 50%、目前排名 #598、勝／敗 1／1，近期戰績包含 `#/matches/2`（手動建立）與 `#/matches/3`（一般匯入）。`#/matches/3` 可讀取 1v1 敗北、#598 → #601、雙方隊伍及保留的原始 JSON，確認資料保存與 Hash 詳情路由均正常。
- 以「清除本機資料」的確認對話框移除上述手動、完整備份與一般 JSON 的所有無敏感測試資料及匯入歷程。清除後匯入頁顯示「尚未匯入任何資料」，首頁回到累計 0、可判定勝率 —、目前排名 —、勝／敗 0／0；GitHub Pages 模擬 origin 未留下測試資料。

## 最終自動化檢查

| 指令 | 結果 |
| --- | --- |
| `pnpm test` | 通過：6 個測試檔、16 個測試。 |
| `pnpm check` | 通過：`tsc --noEmit` 無型別錯誤。 |
| `GITHUB_ACTIONS=true pnpm build` | 通過：產生 `dist/`，並以 `/rf-pvp-analyzer/` 作為 GitHub Pages 資源基底。Vite 僅提出主 JavaScript chunk 超過 500KB 的非阻斷警告。 |

## 4173 `pnpm preview` 專用完整備份入口補驗

- 在重新以一般 `pnpm build` 啟動的 `http://localhost:4173/?static-final-verify=1#/import`，先以頁內確認對話清除既有無敏感測試資料與匯入歷程；頁面回到「尚未匯入任何資料」。
- 由「還原本機備份」按鈕所連結的**隱藏專用檔案欄位**（非 textarea 一般匯入欄位）載入 1 筆無敏感 `rf-pvp-analyzer/local-backup-v1` 測試備份。還原後匯入頁列出「4173 靜態備份還原驗證（測試後清除）／1 已辨識／0 未建立」，證實匯入歷程一併恢復。
- 還原後首頁為累計 **1**、可判定勝率 **100%**、目前排名 **#699**、勝／敗 **1／0**，近期戰績連結為 `#/matches/5`。詳情頁可讀出 1v1 勝利、賽前 #700、賽後 #699、雙方測試隊伍與清理用備註，證實正式 preview 的還原、統計與單筆讀取均正常。
- 詳情頁按「刪除紀錄」會先顯示頁內確認對話；選擇「取消」後仍停留在 `#/matches/5`，且完整測試紀錄、隊伍與備註仍可見。
- 再次確認刪除後頁面回到空的戰績歷史；首頁回復為累計 **0**、勝率 **—**、目前排名 **—**、勝／敗 **0／0**。4173 preview 的測試 IndexedDB 已清理，不含使用者真實資料。

## GitHub Pages 正式部署

- 2026-08-25：儲存庫擁有者已在 GitHub Pages 設定中選擇 **GitHub Actions** 作為部署來源；本專案既有的 `deploy-pages.yml` 未新增 GitHub 提供的範例 workflow。
- 最新手動觸發部署已成功完成：[GitHub Actions run 32830384243](https://github.com/chiaomao666/rf-pvp-analyzer/actions/runs/32830384243)。
- GitHub Pages 已建立且強制 HTTPS：<https://chiaomao666.github.io/rf-pvp-analyzer/>。公開首頁實測可載入總覽、導覽與 Hash 路由入口；該網域是新的 origin，因此首頁顯示 0 筆本機資料，符合 IndexedDB 同源隔離設計。

## 積分欄位相容修正

- 原因：早期已保存的守衛資料雖保留賽前／賽後積分，但本機正規化器尚未把它映射為畫面可讀欄位，所以頁面先前只顯示排名。
- 修正後：既有 11 筆本機資料的總覽顯示目前積分 **6,860**；第一筆詳情顯示賽前 **6,740**、賽後 **6,860**、積分變動 **+120**；戰績歷史已並列「積分變動」與「排名變動」。
- 已以無敏感手動測試紀錄驗證積分輸入、保存、首頁、列表與詳情顯示：首頁「目前積分」為 **1,245**，近期／列表積分變動為 **+45**，詳情頁顯示賽前 **1,200**、賽後 **1,245**、積分變動 **+45**。隨後確認刪除；開發 origin 回復原本 11 筆資料。

## 帳號工作區實作回歸

- IndexedDB schema 已升至 v2，新增帳號 profile store 與每筆戰績／匯入批次的 `profileId`。未選取帳號時，不會新增一般匯入或手動戰績。
- 單元測試覆蓋兩個官方 profile 可使用相同來源鍵而互不更新、跨帳號單筆讀取被遮蔽、舊 v1 未綁定資料必須經顯式遷移、JSON 明確所有者不符會拒絕、v2 備份只允許相符帳號還原，以及登入回應的 password／token 不進入可攜備份。
- `pnpm test`：通過 **6 個測試檔、21 個測試**；`pnpm check`：通過。
- 開發站以 Hash URL `/#/account` 實測可載入帳號工作區頁：遊戲帳號／密碼表單、風險核取方塊、示範模式、既有工作區、未綁定資料轉移提示及 CORS／Cloudflare 說明皆正常顯示。此驗證沒有使用任何真實帳號或密碼，也沒有呼叫有效登入。
- 靜態站的路由必須使用 Hash，例如 `#/account`；直接開啟 `/account` 由靜態主機回退至首頁屬預期行為，並不代表帳號頁路由失效。
- 以使用者原始 `local-backup-v1` 在**獨立 fake IndexedDB 測試程序**完成遷移驗證：原始檔僅讀取、不寫入專案、未上傳或輸出；未選工作區時資料維持未綁定，經明確遷移後，**11 筆**皆屬目標工作區；切換至第二工作區後列表為空。一次性驗證程式已在測試後刪除。

## medals-only 查詢與帳號頁改善

- 新增的 Phoenix 回歸測試以假的 WebSocket 驗證：僅依序送出 `player:<user_id>` 的 `phx_join` 與 `medals`；收到成功回覆時只將 `response.medals` 存成快照，即使回覆同時含有 score、rank、Union 等欄位，快照也不含那些欄位。
- `pnpm test` 通過 **7 個測試檔、25 個測試**；`pnpm check` 及 `GITHUB_ACTIONS=true pnpm build` 均通過。Vite 僅提出既有主 JavaScript chunk 超過 500KB 的非阻斷警告。
- `/#/account` 已以桌面與 390px 行動版截圖核對：密碼欄有明確的「顯示／隱藏」控制，主要面板、表單欄、工作區提示與風險／CORS 資訊格均具有一致的 HUD 外框；測試不涉及任何真實認證資料。
- 2026-08-25 GitHub Pages workflow [32839728930](https://github.com/chiaomao666/rf-pvp-analyzer/actions/runs/32839728930) 已成功部署 commit `a7ff04d`。公開根頁 <https://chiaomao666.github.io/rf-pvp-analyzer/> 回應 200 並載入新入口資源；由根頁站內 Hash 導覽至 `#/account` 後，可見「MEDALS ONLY」資料範圍、密碼「顯示」按鈕，以及所有主要登入、工作區與風險資訊格的外框。
- 在公開帳號頁直接點選「顯示」後，密碼輸入欄由 `type="password"` 切換為 `type="text"`，按鈕文字與提示同步改為「隱藏」；此互動未填入帳密、未送出登入請求。

## CORS 阻擋處理

- 使用者在公開 GitHub Pages 登入頁實測時，瀏覽器主控台明確回報官方 `users/log_in` 回應缺少 `Access-Control-Allow-Origin`，因此此網域的直接登入被 CORS 政策阻擋。這是伺服器對呼叫 origin 的政策，並非帳密驗證結果。
- `loginOfficialAccount` 現在使用可中止的 12 秒限制；CORS／Cloudflare／網路拒絕或逾時都會結束登入流程、清除密碼欄，並以非帳密錯誤提示「請官方加入目前網域的 CORS 允許清單」或「使用自行管理的本機代理」。錯誤登入不會建立 profile 或設定活動工作區。
- 新增 fetch 拒絕回歸案例後，`pnpm test` 通過 **7 個測試檔、26 個測試**；`pnpm check` 與 `GITHUB_ACTIONS=true pnpm build` 均通過。
