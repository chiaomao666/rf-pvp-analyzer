import type { TeamMember } from "@/components/PvpUi";

export type PvpOutcome = "win" | "loss" | "draw" | "unknown";
export type PvpMode = "1v1" | "3v3" | "5v5";
export type LocalProfile = { id: string; externalUserId?: string; kind: "official" | "demo"; createdAt: number; lastVerifiedAt?: number; medalsSnapshot?: { capturedAt: number; count: number; items: unknown[] } };
export type LocalPvpMatch = { id: number; profileId?: string; battleAt: number; mode: PvpMode; outcome: PvpOutcome; playerTeam: TeamMember[]; opponentTeam: TeamMember[]; opponentName?: string; rankBefore?: number; rankAfter?: number; scoreBefore?: number; scoreAfter?: number; notes?: string; sourceBattleChannel?: string; sourceBattleId?: string; rawPayload?: unknown; unrecognizedFields?: Record<string, unknown>; createdAt: number; updatedAt: number };
export type LocalImportBatch = { id: number; profileId?: string; receivedAt: number; label: string; recognizedCount: number; rejectedCount: number; warnings: string[] };
export type MatchInput = Omit<LocalPvpMatch, "id" | "createdAt" | "updatedAt" | "profileId"> & { profileId?: string };
export type MatchFilters = Partial<Pick<LocalPvpMatch, "mode" | "outcome">> & { startAt?: number; endAt?: number };
export type ImportSummary = { importedCount: number; createdCount: number; updatedCount: number; rejectedCount: number; warnings: string[] };
type PortableMatch = Omit<LocalPvpMatch, "id">;
type PortableImport = Omit<LocalImportBatch, "id">;
export type LocalBackup = { format: "rf-pvp-analyzer/local-backup-v1" | "rf-pvp-analyzer/local-backup-v2"; exportedAt: string; recordCount: number; records: PortableMatch[]; imports: PortableImport[]; profile?: LocalProfile };

const DB_NAME = "rf-pvp-analyzer-local"; const DB_VERSION = 2; const MATCHES = "matches"; const IMPORTS = "imports"; const PROFILES = "profiles"; const ACTIVE_PROFILE_KEY = "rf-pvp-active-profile-id";
let memoryActiveProfileId: string | null = null;
function openDatabase(): Promise<IDBDatabase> { return new Promise((resolve, reject) => { const request = indexedDB.open(DB_NAME, DB_VERSION); request.onupgradeneeded = () => { const db = request.result; if (!db.objectStoreNames.contains(MATCHES)) db.createObjectStore(MATCHES, { keyPath: "id", autoIncrement: true }); if (!db.objectStoreNames.contains(IMPORTS)) db.createObjectStore(IMPORTS, { keyPath: "id", autoIncrement: true }); if (!db.objectStoreNames.contains(PROFILES)) db.createObjectStore(PROFILES, { keyPath: "id" }); }; request.onsuccess = () => resolve(request.result); request.onerror = () => reject(request.error ?? new Error("無法開啟瀏覽器本機資料庫。")); }); }
function requestValue<T>(request: IDBRequest<T>): Promise<T> { return new Promise((resolve, reject) => { request.onsuccess = () => resolve(request.result); request.onerror = () => reject(request.error ?? new Error("本機資料庫操作失敗。")); }); }
function transactionDone(transaction: IDBTransaction): Promise<void> { return new Promise((resolve, reject) => { transaction.oncomplete = () => resolve(); transaction.onerror = () => reject(transaction.error ?? new Error("本機資料庫操作失敗。")); transaction.onabort = () => reject(transaction.error ?? new Error("本機資料庫操作已取消。")); }); }
async function withStore<T>(storeName: string, mode: IDBTransactionMode, action: (store: IDBObjectStore) => IDBRequest<T>): Promise<T> { const db = await openDatabase(); try { return await requestValue(action(db.transaction(storeName, mode).objectStore(storeName))); } finally { db.close(); } }
function notifyStoreChange() { if (typeof window !== "undefined") window.dispatchEvent(new Event("rf-pvp-store-change")); }
function notifyAccountChange() { if (typeof window !== "undefined") window.dispatchEvent(new Event("rf-pvp-account-change")); }
function browserStorage(): Storage | null { try { return typeof window !== "undefined" && window.localStorage ? window.localStorage : null; } catch { return null; } }
export function getActiveProfileId() { return browserStorage()?.getItem(ACTIVE_PROFILE_KEY) ?? memoryActiveProfileId; }
export function setActiveProfileId(profileId: string | null) { memoryActiveProfileId = profileId; const local = browserStorage(); if (local) profileId ? local.setItem(ACTIVE_PROFILE_KEY, profileId) : local.removeItem(ACTIVE_PROFILE_KEY); notifyAccountChange(); notifyStoreChange(); }
function asObject(value: unknown): Record<string, unknown> | null { return value && typeof value === "object" && !Array.isArray(value) ? value as Record<string, unknown> : null; }
function first(record: Record<string, unknown>, keys: string[]) { return keys.map(key => record[key]).find(value => value !== undefined && value !== null); }
function timestamp(value: unknown) { if (typeof value === "number" && Number.isFinite(value)) return value > 10_000_000_000 ? Math.trunc(value) : Math.trunc(value * 1000); if (typeof value === "string") { const parsed = Date.parse(value); return Number.isNaN(parsed) ? null : parsed; } return null; }
function positiveInteger(value: unknown) { const numeric = typeof value === "number" ? value : Number(value); return Number.isInteger(numeric) && numeric > 0 ? numeric : undefined; }
function scoreValue(value: unknown) { const numeric = typeof value === "number" ? value : typeof value === "string" && value.trim() ? Number(value) : Number.NaN; return Number.isFinite(numeric) && numeric >= 0 ? numeric : undefined; }
function sourceId(value: unknown) { return typeof value === "string" && value.trim() ? value.trim() : typeof value === "number" && Number.isFinite(value) ? String(Math.trunc(value)) : undefined; }
function outcome(value: unknown): PvpOutcome { const text = String(value ?? "").toLowerCase(); if (["win", "won", "victory", "勝", "勝利", "true"].includes(text)) return "win"; if (["loss", "lose", "lost", "defeat", "敗", "失敗", "false"].includes(text)) return "loss"; return ["draw", "tie", "平手"].includes(text) ? "draw" : "unknown"; }
function team(value: unknown): TeamMember[] { const entries = Array.isArray(value) ? value : value == null ? [] : [value]; return entries.flatMap(entry => { if (typeof entry === "string" && entry.trim()) return [{ name: entry.trim() }]; const member = asObject(entry); if (!member) return []; const character = asObject(member.character); const name = first(member, ["name", "characterName", "character_name", "displayName"]) ?? character?.name; if (typeof name !== "string" || !name.trim()) return []; const power = first(member, ["power", "combatPower", "combat_power"]); const rawPower = typeof power === "number" && Number.isFinite(power) ? Math.trunc(power) : undefined; return [{ name: name.trim(), ...(positiveInteger(first(member, ["level", "lv"])) ? { level: positiveInteger(first(member, ["level", "lv"])) } : {}), ...(rawPower !== undefined ? { power: rawPower } : {}), ...(typeof first(member, ["role", "class", "abbr"]) === "string" ? { role: String(first(member, ["role", "class", "abbr"])) } : {}), ...(typeof first(member, ["rarity", "grade"]) === "string" ? { rarity: String(first(member, ["rarity", "grade"])) } : {}) }]; }); }
function candidates(root: unknown): unknown[] { if (Array.isArray(root)) return root; const object = asObject(root); if (!object) return []; for (const key of ["matches", "records", "data", "logs"]) if (Array.isArray(object[key])) return object[key] as unknown[]; return [root]; }
function ownerIds(root: unknown) { const values = [asObject(root), ...candidates(root).map(asObject)].filter((value): value is Record<string, unknown> => value !== null); return Array.from(new Set(values.map(value => sourceId(first(value, ["userId", "user_id", "playerId", "player_id"]))).filter((value): value is string => Boolean(value)))); }
function recoverLegacyScores(record: LocalPvpMatch): LocalPvpMatch { const sources = [record as unknown as Record<string, unknown>, asObject(record.unrecognizedFields), asObject(record.rawPayload)].filter((value): value is Record<string, unknown> => value !== null); const findScore = (keys: string[]) => { for (const source of sources) { const value = scoreValue(first(source, keys)); if (value !== undefined) return value; } return undefined; }; const scoreBefore = record.scoreBefore ?? findScore(["scoreBefore", "score_before", "preScore", "pre_score", "beforeScore", "before_score"]); const scoreAfter = record.scoreAfter ?? findScore(["scoreAfter", "score_after", "postScore", "post_score", "afterScore", "after_score"]); return { ...record, ...(scoreBefore !== undefined ? { scoreBefore } : {}), ...(scoreAfter !== undefined ? { scoreAfter } : {}) }; }
export function parsePvpJson(dataText: string): { records: MatchInput[]; rejectedCount: number; warnings: string[]; ownerIds: string[] } { let root: unknown; try { root = JSON.parse(dataText); } catch { throw new Error("JSON 格式無法解析；請確認匯出內容完整且未被截斷。"); } const records: MatchInput[] = []; const warnings: string[] = []; let rejectedCount = 0; for (const candidate of candidates(root)) { const record = asObject(candidate); if (!record) { rejectedCount += 1; continue; } const playerTeam = team(first(record, ["playerTeam", "player_team", "myTeam", "my_team", "team"])); const opponentTeam = team(first(record, ["opponentTeam", "opponent_team", "enemyTeam", "enemy_team", "opponent"])); const battleAt = timestamp(first(record, ["battleAt", "battle_at", "timestamp", "date", "playedAt", "played_at"])); const rawMode = String(first(record, ["mode", "battleMode", "battle_mode", "type"]) ?? "").toLowerCase().replaceAll(" ", ""); const mode: PvpMode | null = rawMode.includes("1v1") || rawMode === "1" || playerTeam.length === 1 ? "1v1" : rawMode.includes("3v3") || rawMode === "3" || playerTeam.length === 3 ? "3v3" : null; if (!battleAt || !mode || !playerTeam.length || !opponentTeam.length || playerTeam.length > 20 || opponentTeam.length > 20) { rejectedCount += 1; continue; } const scoreBefore = scoreValue(first(record, ["scoreBefore", "score_before", "preScore", "pre_score", "beforeScore", "before_score"])); const scoreAfter = scoreValue(first(record, ["scoreAfter", "score_after", "postScore", "post_score", "afterScore", "after_score"])); records.push({ battleAt, mode, outcome: outcome(first(record, ["outcome", "result", "winner", "status"])), playerTeam, opponentTeam, ...(typeof first(record, ["opponentName", "opponent_name"]) === "string" ? { opponentName: String(first(record, ["opponentName", "opponent_name"])) } : {}), ...(positiveInteger(first(record, ["rankBefore", "rank_before", "preRank", "pre_rank"])) ? { rankBefore: positiveInteger(first(record, ["rankBefore", "rank_before", "preRank", "pre_rank"])) } : {}), ...(positiveInteger(first(record, ["rankAfter", "rank_after", "postRank", "post_rank"])) ? { rankAfter: positiveInteger(first(record, ["rankAfter", "rank_after", "postRank", "post_rank"])) } : {}), ...(scoreBefore !== undefined ? { scoreBefore } : {}), ...(scoreAfter !== undefined ? { scoreAfter } : {}), ...(sourceId(first(record, ["sourceBattleChannel", "source_battle_channel"])) ? { sourceBattleChannel: sourceId(first(record, ["sourceBattleChannel", "source_battle_channel"])) } : {}), ...(sourceId(first(record, ["sourceBattleId", "source_battle_id"])) ? { sourceBattleId: sourceId(first(record, ["sourceBattleId", "source_battle_id"])) } : {}), rawPayload: record }); } if (rejectedCount) warnings.push(`${rejectedCount} 筆資料缺少可辨識的時間、模式或非空雙方隊伍，未建立戰績。`); const recognisedOwners = ownerIds(root); if (!recognisedOwners.length) warnings.push("此 JSON 未提供可辨識的帳號 ID；匯入時將只依目前工作區歸屬，無法證明來源帳號。 "); return { records, rejectedCount, warnings, ownerIds: recognisedOwners }; }
function belongsToProfile(entry: { profileId?: string }, profileId = getActiveProfileId()) { return profileId ? entry.profileId === profileId : !entry.profileId; }
function requireActiveProfileId() { const profileId = getActiveProfileId(); if (!profileId) throw new Error("請先登入或選取帳號工作區，再建立或匯入戰績。 "); return profileId; }
export async function getProfile(profileId: string) { return withStore<LocalProfile | undefined>(PROFILES, "readonly", store => store.get(profileId)); }
export async function listProfiles() { const profiles = await withStore<LocalProfile[]>(PROFILES, "readonly", store => store.getAll()); return profiles.sort((a, b) => (b.lastVerifiedAt ?? 0) - (a.lastVerifiedAt ?? 0) || b.createdAt - a.createdAt); }
export async function upsertProfile(profile: LocalProfile) { await withStore(PROFILES, "readwrite", store => store.put(profile)); notifyAccountChange(); return profile; }
export async function countUnscopedData() { const [matches, imports] = await Promise.all([withStore<LocalPvpMatch[]>(MATCHES, "readonly", store => store.getAll()), withStore<LocalImportBatch[]>(IMPORTS, "readonly", store => store.getAll())]); return { matches: matches.filter(item => !item.profileId).length, imports: imports.filter(item => !item.profileId).length }; }
export async function migrateUnscopedDataToProfile(profileId = requireActiveProfileId()) { const db = await openDatabase(); try { const transaction = db.transaction([MATCHES, IMPORTS], "readwrite"); const matchStore = transaction.objectStore(MATCHES); const importStore = transaction.objectStore(IMPORTS); const [matches, imports] = await Promise.all([requestValue<LocalPvpMatch[]>(matchStore.getAll()), requestValue<LocalImportBatch[]>(importStore.getAll())]); let migratedMatches = 0; let migratedImports = 0; for (const match of matches) if (!match.profileId) { matchStore.put({ ...match, profileId }); migratedMatches += 1; } for (const batch of imports) if (!batch.profileId) { importStore.put({ ...batch, profileId }); migratedImports += 1; } await transactionDone(transaction); notifyStoreChange(); return { matches: migratedMatches, imports: migratedImports }; } finally { db.close(); } }
export async function listMatches(filters: MatchFilters = {}) { const entries = (await withStore<LocalPvpMatch[]>(MATCHES, "readonly", store => store.getAll())).map(recoverLegacyScores); return entries.filter(match => belongsToProfile(match) && (!filters.mode || match.mode === filters.mode) && (!filters.outcome || match.outcome === filters.outcome) && (!filters.startAt || match.battleAt >= filters.startAt) && (!filters.endAt || match.battleAt <= filters.endAt)).sort((a, b) => b.battleAt - a.battleAt); }
export async function getMatch(id: number) { const match = await withStore<LocalPvpMatch | undefined>(MATCHES, "readonly", store => store.get(id)); return match && belongsToProfile(match) ? recoverLegacyScores(match) : undefined; }
export async function saveMatch(input: MatchInput, allowUnscoped = false) { const activeProfileId = getActiveProfileId(); if (!activeProfileId && !allowUnscoped) requireActiveProfileId(); if (activeProfileId && input.profileId && input.profileId !== activeProfileId) throw new Error("戰績帳號與目前工作區不一致。 "); const now = Date.now(); const id = await withStore<IDBValidKey>(MATCHES, "readwrite", store => store.add({ ...input, ...(activeProfileId ? { profileId: activeProfileId } : {}), createdAt: now, updatedAt: now })); notifyStoreChange(); return Number(id); }
export async function deleteMatch(id: number) { const match = await withStore<LocalPvpMatch | undefined>(MATCHES, "readonly", store => store.get(id)); if (!match || !belongsToProfile(match)) throw new Error("找不到目前帳號工作區中的戰績。 "); await withStore(MATCHES, "readwrite", store => store.delete(id)); notifyStoreChange(); }
export async function listImports() { const entries = await withStore<LocalImportBatch[]>(IMPORTS, "readonly", store => store.getAll()); return entries.filter(entry => belongsToProfile(entry)).sort((a, b) => b.receivedAt - a.receivedAt); }
function sameSource(a: LocalPvpMatch, b: MatchInput) { return Boolean(a.sourceBattleChannel && a.sourceBattleId && a.sourceBattleChannel === b.sourceBattleChannel && a.sourceBattleId === b.sourceBattleId); }
export async function importPvpJson(label: string, dataText: string): Promise<ImportSummary> { const profileId = requireActiveProfileId(); const profile = await getProfile(profileId); if (!profile) throw new Error("目前帳號工作區不存在，請重新登入或選取帳號。 "); const parsed = parsePvpJson(dataText); if (profile.externalUserId && parsed.ownerIds.some(ownerId => ownerId !== profile.externalUserId)) throw new Error("JSON 內的帳號 ID 與目前登入帳號不一致，為避免混用已停止匯入。 "); const existing = await listMatches(); let createdCount = 0; let updatedCount = 0; for (const record of parsed.records) { const found = existing.find(match => sameSource(match, record)); if (found) { await withStore(MATCHES, "readwrite", store => store.put({ ...found, ...record, profileId, id: found.id, createdAt: found.createdAt, updatedAt: Date.now() })); updatedCount += 1; } else { await saveMatch({ ...record, profileId }); createdCount += 1; } } await withStore(IMPORTS, "readwrite", store => store.add({ profileId, receivedAt: Date.now(), label: label.trim() || "未命名 JSON 匯入", recognizedCount: parsed.records.length, rejectedCount: parsed.rejectedCount, warnings: parsed.warnings })); notifyStoreChange(); return { importedCount: parsed.records.length, createdCount, updatedCount, rejectedCount: parsed.rejectedCount, warnings: parsed.warnings }; }
function portable(record: LocalPvpMatch): PortableMatch { const { id: _id, ...rest } = record; return rest; }
export async function exportLocalBackup(): Promise<LocalBackup> { const profileId = getActiveProfileId(); const profile = profileId ? await getProfile(profileId) : undefined; const records = (await listMatches()).map(portable); const imports = (await listImports()).map(({ id: _id, ...batch }) => batch); return { format: profile ? "rf-pvp-analyzer/local-backup-v2" : "rf-pvp-analyzer/local-backup-v1", exportedAt: new Date().toISOString(), recordCount: records.length, records, imports, ...(profile ? { profile } : {}) }; }
export async function restoreLocalBackup(dataText: string, replace = false): Promise<{ restored: number; skipped: number }> { let backup: unknown; try { backup = JSON.parse(dataText); } catch { throw new Error("備份 JSON 無法解析。 "); } const object = asObject(backup); const isV1 = object?.format === "rf-pvp-analyzer/local-backup-v1"; const isV2 = object?.format === "rf-pvp-analyzer/local-backup-v2"; if (!object || (!isV1 && !isV2) || !Array.isArray(object.records)) throw new Error("這不是可還原的 RF PVP 本機備份檔。 "); const activeProfileId = getActiveProfileId(); const backupProfile = asObject(object.profile) as LocalProfile | null; if (isV2 && (!backupProfile?.id || !activeProfileId || backupProfile.id !== activeProfileId)) throw new Error("此完整備份屬於其他帳號工作區；請先登入相符帳號後再還原。 "); if (replace) await clearLocalData(); let restored = 0; let skipped = 0; const existing = await listMatches(); for (const entry of object.records) { const record = asObject(entry); if (!record || typeof record.battleAt !== "number" || (record.mode !== "1v1" && record.mode !== "3v3") || !Array.isArray(record.playerTeam) || !Array.isArray(record.opponentTeam)) { skipped += 1; continue; } const { id: _id, createdAt: _createdAt, updatedAt: _updatedAt, profileId: recordProfileId, ...input } = record; if (isV2 && recordProfileId && recordProfileId !== activeProfileId) throw new Error("完整備份中的戰績帳號不一致，已停止還原。 "); if (existing.some(match => sameSource(match, input as MatchInput))) { skipped += 1; continue; } await saveMatch({ ...(input as MatchInput), ...(activeProfileId ? { profileId: activeProfileId } : {}) }, !activeProfileId); restored += 1; } if (Array.isArray(object.imports)) for (const batch of object.imports) { const value = asObject(batch); if (value && typeof value.label === "string") await withStore(IMPORTS, "readwrite", store => store.add({ ...(activeProfileId ? { profileId: activeProfileId } : {}), receivedAt: typeof value.receivedAt === "number" ? value.receivedAt : Date.now(), label: value.label, recognizedCount: typeof value.recognizedCount === "number" ? value.recognizedCount : 0, rejectedCount: typeof value.rejectedCount === "number" ? value.rejectedCount : 0, warnings: Array.isArray(value.warnings) ? value.warnings.filter(item => typeof item === "string") : [] })); } notifyStoreChange(); return { restored, skipped }; }
export async function clearLocalData() { const activeProfileId = getActiveProfileId(); const db = await openDatabase(); try { const transaction = db.transaction([MATCHES, IMPORTS], "readwrite"); const matchStore = transaction.objectStore(MATCHES); const importStore = transaction.objectStore(IMPORTS); const [matches, imports] = await Promise.all([requestValue<LocalPvpMatch[]>(matchStore.getAll()), requestValue<LocalImportBatch[]>(importStore.getAll())]); for (const match of matches) if (belongsToProfile(match, activeProfileId)) matchStore.delete(match.id); for (const batch of imports) if (belongsToProfile(batch, activeProfileId)) importStore.delete(batch.id); await transactionDone(transaction); } finally { db.close(); } notifyStoreChange(); }
export async function dashboard() { const records = await listMatches(); const wins = records.filter(item => item.outcome === "win").length; const losses = records.filter(item => item.outcome === "loss").length; const decided = wins + losses; const ordered = [...records].sort((a, b) => a.battleAt - b.battleAt); const ranked = ordered.filter(item => item.rankAfter); const scored = ordered.filter(item => item.scoreAfter !== undefined); return { total: records.length, wins, losses, winRate: decided ? Math.round((wins / decided) * 1000) / 10 : null, currentRank: ranked.at(-1)?.rankAfter ?? null, currentScore: scored.at(-1)?.scoreAfter ?? null, rankSeries: ranked.map(item => ({ battleAt: item.battleAt, rank: item.rankAfter! })), scoreSeries: scored.map(item => ({ battleAt: item.battleAt, score: item.scoreAfter! })), recent: records.slice(0, 6) }; }
